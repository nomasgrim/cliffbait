"use client";

import Slider from "@/components/Slider";

export default function HomeClient() {
  return (
    <section>
      {/* LCP HERO ELEMENT */}
      <div className="w-full">
        <Slider />
      </div>
    </section>
  );
}